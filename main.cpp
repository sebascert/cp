// vim X.cpp in && g++ -Wall -fsanitize=address,undefined X.cpp && ./a.out <in

#include <bits/stdc++.h>
using namespace std;

typedef long long lli;
typedef long double ld;
typedef pair<lli, lli> ii;
typedef vector<lli> vi;

#define endl '\n'
#define _ ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define f first
#define s second
#define pb push_back
#define sz(c) (lli) c.size()
#define all(c) c.begin(), c.end()
#define fore(i, a, b) for (lli i = (a), I = (b); i < I; i++)
#define ford(i, a, b) for (lli i = (a), I = (b); i-- > I;)

#define perr(x) cerr << #x << " " << (x) << endl

const lli INF = 1e18, NINF = -1e18;

int main() {_

	return 0;
}
